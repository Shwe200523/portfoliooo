 const tl=gsap.timeline(); 
     tl.from(".aniheader",{
         opacity:0,
        y:-30,
       duration:1,
        stagger:0.1,
      ease: "power2.inOut",
     
     })
                
                   
